You are making a SINGLE targeted change to an existing Next.js website.

The website is already built and deployed. Your job is ONLY to apply this specific change:

"Cambia el color del botón principal del hero a verde lima (#84cc16) y agrega un badge "14 días gratis" encima del botón."

RULES:
- Work on the 'dev' branch (already checked out)
- Apply ONLY the requested change — do not refactor, redesign, or clean up anything else
- If you need to install a new package, do so — but only if the change truly requires it
- Run: npm run build
- If it passes: commit all changed files with message "change: Cambia el color del botón principal del hero a verde lima (#" and push
- If build errors: fix ONLY the errors, then commit and push
- DO NOT rewrite files that are not related to this change


## Current file contents (read these — do NOT use Read/LS/Glob tools, they are already provided):

### app/page.tsx
```tsx
"use client"

import { useState } from 'react'
import {
  Menu,
  X,
  ArrowRight,
  Kanban,
  Clock,
  BarChart3,
  MessageSquare,
  Github,
  Users,
  LayoutDashboard,
  Bell,
  Check,
  ChevronDown,
  ChevronUp,
  Star,
  Zap,
  Shield,
  Globe,
  Twitter,
  Linkedin,
  Mail,
  ChevronLeft,
  ChevronRight
} from 'lucide-react'

export default function HomePage() {
  const [mobileNavOpen, setMobileNavOpen] = useState(false)
  const [openFaq, setOpenFaq] = useState<number | null>(0)
  const [testimonialIndex, setTestimonialIndex] = useState(0)

  const navLinks = [
    { label: 'Features', href: '#features' },
    { label: 'Pricing', href: '#pricing' },
    { label: 'Testimonials', href: '#testimonials' },
    { label: 'FAQ', href: '#faq' }
  ]

  const stats = [
    { value: '50K+', label: 'Active Teams' },
    { value: '2M+', label: 'Projects Managed' },
    { value: '99.9%', label: 'Uptime SLA' },
    { value: '4.9/5', label: 'Customer Rating' }
  ]

  const features = [
    {
      icon: Kanban,
      title: 'Drag and Drop Kanban',
      description: 'Visualize your workflow with intuitive boards. Move tasks effortlessly and keep your team aligned on priorities.',
      size: 'large'
    },
    {
      icon: Clock,
      title: 'Time Tracking',
      description: 'Built in timers that integrate seamlessly with your tasks. Know exactly where your time goes.',
      size: 'small'
    },
    {
      icon: BarChart3,
      title: 'Automated Reports',
      description: 'Generate custom reports automatically. Track velocity, burndown, and team performance.',
      size: 'small'
    },
    {
      icon: MessageSquare,
      title: 'Slack Integration',
      description: 'Get notifications, create tasks, and update statuses directly from Slack without switching contexts.',
      size: 'medium'
    },
    {
      icon: Github,
      title: 'GitHub Sync',
      description: 'Link commits, PRs, and issues to tasks. Keep development and project management in perfect sync.',
      size: 'medium'
    },
    {
      icon: Users,
      title: 'Team Management',
      description: 'Granular permissions and role based access control for teams of any size.',
      size: 'small'
    },
    {
      icon: LayoutDashboard,
      title: 'Productivity Dashboard',
      description: 'Real time insights into your team productivity with customizable widgets.',
      size: 'small'
    },
    {
      icon: Bell,
      title: 'Smart Notifications',
      description: 'AI powered alerts that surface what matters most without the noise.',
      size: 'large'
    }
  ]

  const services = [
    {
      icon: Zap,
      title: 'Quick Setup',
      description: 'Get your team onboarded in minutes with our guided setup wizard and pre built templates.'
    },
    {
      icon: Shield,
      title: 'Enterprise Security',
      description: 'SOC 2 compliant with SSO, 2FA, and advanced audit logs for complete peace of mind.'
    },
    {
      icon: Globe,
      title: 'Global Collaboration',
      description: 'Built for remote teams across time zones with async friendly features and real time sync.'
    }
  ]

  const plans = [
    {
      name: 'Starter',
      price: '$29',
      period: 'per month',
      description: 'Perfect for small teams getting started',
      features: [
        'Up to 10 team members',
        'Unlimited projects',
        'Basic kanban boards',
        'Time tracking',
        'Email support',
        'Standard integrations'
      ],
      cta: 'Start Free Trial',
      highlighted: false
    },
    {
      name: 'Professional',
      price: '$79',
      period: 'per month',
      description: 'For growing teams that need more power',
      features: [
        'Up to 50 team members',
        'Advanced analytics',
        'Custom workflows',
        'Slack and GitHub integration',
        'Priority support',
        'API access',
        'Advanced permissions'
      ],
      cta: 'Start Free Trial',
      highlighted: true
    },
    {
      name: 'Enterprise',
      price: 'Custom',
      period: 'contact sales',
      description: 'For large organizations with specific needs',
      features: [
        'Unlimited team members',
        'Dedicated account manager',
        'Custom integrations',
        'SSO and SAML',
        'Advanced security',
        'SLA guarantee',
        'On premise option',
        'Training and onboarding'
      ],
      cta: 'Contact Sales',
      highlighted: false
    }
  ]

  const testimonials = [
    {
      quote: 'FlowDesk transformed how our remote team collaborates. The Slack integration alone saved us hours every week.',
      role: 'Engineering Director',
      company: 'Tech Startup',
      rating: 5
    },
    {
      quote: 'The automated reports feature is a game changer. Stakeholder updates that used to take hours now happen automatically.',
      role: 'Product Manager',
      company: 'SaaS Company',
      rating: 5
    },
    {
      quote: 'We evaluated dozens of tools. FlowDesk was the only one that combined kanban, time tracking, and GitHub integration seamlessly.',
      role: 'CTO',
      company: 'Digital Agency',
      rating: 5
    },
    {
      quote: 'Our team productivity increased by 40% after switching to FlowDesk. The dashboard gives us visibility we never had before.',
      role: 'Operations Lead',
      company: 'Remote First Company',
      rating: 5
    }
  ]

  const faqs = [
    {
      question: 'How does the free trial work?',
      answer: 'Start with a 14 day free trial on any plan. No credit card required. You get full access to all features during the trial period. Cancel anytime with no obligations.'
    },
    {
      question: 'Can I migrate my existing projects from other tools?',
      answer: 'Yes! FlowDesk supports imports from Trello, Asana, Jira, Monday, and more. Our migration wizard guides you through the process, and our support team is available to help with complex migrations.'
    },
    {
      question: 'How does the GitHub integration work?',
      answer: 'Connect your GitHub repositories to automatically link commits, pull requests, and issues to FlowDesk tasks. Changes sync in real time, and you can create tasks directly from GitHub issues.'
    },
    {
      question: 'Is my data secure?',
      answer: 'Absolutely. FlowDesk is SOC 2 Type II certified. We use end to end encryption, offer two factor authentication, and provide detailed audit logs. Enterprise plans include SSO and SAML support.'
    },
    {
      question: 'Can I upgrade or downgrade my plan at any time?',
      answer: 'Yes, you can change your plan at any time. Upgrades take effect immediately, and downgrades apply at your next billing cycle. Prorated credits are applied automatically.'
    },
    {
      question: 'Do you offer discounts for nonprofits or education?',
      answer: 'We offer special pricing for nonprofits, educational institutions, and open source projects. Contact our sales team to learn more about qualifying programs.'
    }
  ]

  const nextTestimonial = () => {
    setTestimonialIndex((prev) => (prev + 1) % testimonials.length)
  }

  const prevTestimonial = () => {
    setTestimonialIndex((prev) => (prev - 1 + testimonials.length) % testimonials.length)
  }

  return (
    <div className="min-h-screen" style={{ backgroundColor: '#0A1628', color: '#F8FAFC' }}>
      {/* Sticky Navigation */}
      <nav className="fixed top-0 left-0 right-0 z-50 backdrop-blur-lg border-b" style={{ backgroundColor: 'rgba(10, 22, 40, 0.9)', borderColor: 'rgba(100, 116, 139, 0.2)' }}>
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div className="flex items-center justify-between h-16">
            <a href="#" className="flex items-center gap-2">
              <div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#84CC16' }}>
                <Kanban className="w-5 h-5" style={{ color: '#0A1628' }} />
              </div>
              <span className="text-xl font-bold" style={{ fontFamily: 'Inter, sans-serif' }}>FlowDesk</span>
            </a>

            {/* Desktop Nav */}
            <div className="hidden md:flex items-center gap-8">
              {navLinks.map((link) => (
                <a
                  key={link.label}
                  href={link.href}
                  className="text-sm font-medium transition-colors hover:opacity-80"
                  style={{ color: '#F8FAFC' }}
                >
                  {link.label}
                </a>
              ))}
            </div>

            <div className="hidden md:flex items-center gap-4">
              <a
                href="mailto:hello@flowdesk.io"
                className="text-sm font-medium transition-colors hover:opacity-80"
                style={{ color: '#64748B' }}
              >
                Sign In
              </a>
              <a
                href="#pricing"
                className="px-4 py-2 rounded-lg text-sm font-semibold transition-all hover:scale-105"
                style={{ backgroundColor: '#84CC16', color: '#0A1628' }}
              >
                Start Free Trial
              </a>
            </div>

            {/* Mobile Menu Button */}
            <button
              className="md:hidden p-2"
              onClick={() => setMobileNavOpen(!mobileNavOpen)}
              aria-label="Toggle menu"
            >
              {mobileNavOpen ? <X className="w-6 h-6" /> : <Menu className="w-6 h-6" />}
            </button>
          </div>
        </div>

        {/* Mobile Nav */}
        <div
          className={`md:hidden overflow-hidden transition-all duration-500 ${
            mobileNavOpen ? 'max-h-96 opacity-100' : 'max-h-0 opacity-0'
          }`}
          style={{ backgroundColor: '#132240' }}
        >
          <div className="px-4 py-4 space-y-4">
            {navLinks.map((link) => (
              <a
                key={link.label}
                href={link.href}
                className="block text-sm font-medium py-2"
                onClick={() => setMobileNavOpen(false)}
              >
                {link.label}
              </a>
            ))}
            <div className="pt-4 border-t" style={{ borderColor: 'rgba(100, 116, 139, 0.2)' }}>
              <a
                href="mailto:hello@flowdesk.io"
                className="block text-sm font-medium py-2"
                style={{ color: '#64748B' }}
              >
                Sign In
              </a>
              <a
                href="#pricing"
                className="block w-full text-center px-4 py-2 rounded-lg text-sm font-semibold mt-2"
                style={{ backgroundColor: '#84CC16', color: '#0A1628' }}
                onClick={() => setMobileNavOpen(false)}
              >
                Start Free Trial
              </a>
            </div>
          </div>
        </div>
      </nav>

      {/* Hero Section */}
      <section className="pt-32 pb-20 px-4 sm:px-6 lg:px-8 relative overflow-hidden">
        <div
          className="absolute inset-0 opacity-30"
          style={{
            background: 'radial-gradient(ellipse at center top, rgba(132, 204, 22, 0.15), transparent 60%)'
          }}
        />
        <div className="max-w-4xl mx-auto text-center relative z-10">
          <div
            className="inline-flex items-center gap-2 px-4 py-2 rounded-full text-sm font-medium mb-8"
            style={{ backgroundColor: 'rgba(132, 204, 22, 0.1)', color: '#84CC16' }}
          >
            <Zap className="w-4 h-4" />
            Now with AI powered insights
          </div>
          <h1
            className="text-4xl sm:text-5xl lg:text-6xl font-bold leading-tight mb-6"
            style={{ fontFamily: 'Inter, sans-serif' }}
          >
            Project management for{' '}
            <span style={{ color: '#84CC16' }}>remote teams</span>
          </h1>
          <p
            className="text-lg sm:text-xl mb-10 max-w-2xl mx-auto"
            style={{ color: '#64748B' }}
          >
            Kanban boards, time tracking, automated reports, and seamless integrations with Slack and GitHub. Everything your distributed team needs in one place.
          </p>
          <div className="flex flex-col sm:flex-row items-center justify-center gap-4">
            <a
              href="#pricing"
              className="w-full sm:w-auto px-8 py-4 rounded-lg text-base font-semibold transition-all hover:scale-105 flex items-center justify-center gap-2"
              style={{ backgroundColor: '#84CC16', color: '#0A1628' }}
            >
              Start Free Trial
              <ArrowRight className="w-5 h-5" />
            </a>
            <a
              href="#features"
              className="w-full sm:w-auto px-8 py-4 rounded-lg text-base font-semibold transition-all hover:opacity-80 border"
              style={{ borderColor: 'rgba(100, 116, 139, 0.3)', color: '#F8FAFC' }}
            >
              See How It Works
            </a>
          </div>

          {/* Dashboard Mockup */}
          <div className="mt-16 relative">
            <div
              className="rounded-2xl p-1"
              style={{
                background: 'linear-gradient(135deg, rgba(132, 204, 22, 0.3), rgba(19, 34, 64, 0.8))'
              }}
            >
              <div
                className="rounded-xl p-6 backdrop-blur-sm"
                style={{ backgroundColor: 'rgba(19, 34, 64, 0.9)' }}
              >
                <div className="flex items-center gap-2 mb-4">
                  <div className="w-3 h-3 rounded-full" style={{ backgroundColor: '#ef4444' }} />
                  <div className="w-3 h-3 rounded-full" style={{ backgroundColor: '#f59e0b' }} />
                  <div className="w-3 h-3 rounded-full" style={{ backgroundColor: '#22c55e' }} />
                </div>
                <div className="grid grid-cols-4 gap-4">
                  {['To Do', 'In Progress', 'Review', 'Done'].map((col, i) => (
                    <div key={col} className="space-y-3">
                      <div className="flex items-center justify-between">
                        <span className="text-sm font-medium" style={{ color: '#64748B' }}>{col}</span>
                        <span className="text-xs px-2 py-0.5 rounded" style={{ backgroundColor: 'rgba(100, 116, 139, 0.2)' }}>
                          {[4, 3, 2, 5][i]}
                        </span>
                      </div>
                      {Array(Math.min([4, 3, 2, 5][i], 3)).fill(0).map((_, j) => (
                        <div
                          key={j}
                          className="p-3 rounded-lg"
                          style={{ backgroundColor: 'rgba(100, 116, 139, 0.1)' }}
                        >
                          <div className="h-2 rounded w-3/4 mb-2" style={{ backgroundColor: 'rgba(100, 116, 139, 0.3)' }} />
                          <div className="h-2 rounded w-1/2" style={{ backgroundColor: 'rgba(100, 116, 139, 0.2)' }} />
                        </div>
                      ))}
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Stats Banner */}
      <section className="py-12 px-4 sm:px-6 lg:px-8 border-y" style={{ borderColor: 'rgba(100, 116, 139, 0.2)' }}>
        <div className="max-w-7xl mx-auto">
          <div className="grid grid-cols-2 md:grid-cols-4 gap-8">
            {stats.map((stat) => (
              <div key={stat.label} className="text-center">
                <div className="text-3xl sm:text-4xl font-bold mb-2" style={{ color: '#84CC16' }}>
                  {stat.value}
                </div>
                <div className="text-sm" style={{ color: '#64748B' }}>{stat.label}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Features Bento Grid */}
      <section id="features" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
              Everything you need to ship faster
            </h2>
            <p className="text-lg max-w-2xl mx-auto" style={{ color: '#64748B' }}>
              Powerful features designed for modern remote teams
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
            {features.map((feature, index) => {
              const Icon = feature.icon
              const isLarge = feature.size === 'large'
              const isMedium = feature.size === 'medium'
              return (
                <div
                  key={feature.title}
                  className={`p-6 rounded-2xl border transition-all hover:border-opacity-50 ${
                    isLarge ? 'lg:col-span-2' : isMedium ? 'lg:col-span-2' : ''
                  }`}
                  style={{
                    backgroundColor: 'rgba(19, 34, 64, 0.5)',
                    borderColor: 'rgba(100, 116, 139, 0.2)',
                    backdropFilter: 'blur(10px)'
                  }}
                >
                  <div
                    className="w-12 h-12 rounded-xl flex items-center justify-center mb-4"
                    style={{ backgroundColor: 'rgba(132, 204, 22, 0.1)' }}
                  >
                    <Icon className="w-6 h-6" style={{ color: '#84CC16' }} />
                  </div>
                  <h3 className="text-xl font-semibold mb-2">{feature.title}</h3>
                  <p style={{ color: '#64748B' }}>{feature.description}</p>
                </div>
              )
            })}
          </div>
        </div>
      </section>

      {/* Services Cards */}
      <section className="py-20 px-4 sm:px-6 lg:px-8" style={{ backgroundColor: '#132240' }}>
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
              Built for teams that move fast
            </h2>
            <p className="text-lg max-w-2xl mx-auto" style={{ color: '#64748B' }}>
              From setup to scale, we have got you covered
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
            {services.map((service) => {
              const Icon = service.icon
              return (
                <div
                  key={service.title}
                  className="p-8 rounded-2xl border transition-all hover:translate-y-[-4px]"
                  style={{
                    backgroundColor: 'rgba(10, 22, 40, 0.5)',
                    borderColor: 'rgba(100, 116, 139, 0.2)'
                  }}
                >
                  <div
                    className="w-14 h-14 rounded-xl flex items-center justify-center mb-6"
                    style={{ backgroundColor: 'rgba(132, 204, 22, 0.1)' }}
                  >
                    <Icon className="w-7 h-7" style={{ color: '#84CC16' }} />
                  </div>
                  <h3 className="text-xl font-semibold mb-3">{service.title}</h3>
                  <p style={{ color: '#64748B' }}>{service.description}</p>
                </div>
              )
            })}
          </div>
        </div>
      </section>

      {/* Pricing Table */}
      <section id="pricing" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-7xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
              Simple, transparent pricing
            </h2>
            <p className="text-lg max-w-2xl mx-auto" style={{ color: '#64748B' }}>
              Start free for 14 days. No credit card required.
            </p>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-5xl mx-auto">
            {plans.map((plan) => (
              <div
                key={plan.name}
                className={`p-8 rounded-2xl border relative ${
                  plan.highlighted ? 'scale-105' : ''
                }`}
                style={{
                  backgroundColor: plan.highlighted ? 'rgba(132, 204, 22, 0.05)' : 'rgba(19, 34, 64, 0.5)',
                  borderColor: plan.highlighted ? '#84CC16' : 'rgba(100, 116, 139, 0.2)'
                }}
              >
                {plan.highlighted && (
                  <div
                    className="absolute -top-4 left-1/2 -translate-x-1/2 px-4 py-1 rounded-full text-sm font-semibold"
                    style={{ backgroundColor: '#84CC16', color: '#0A1628' }}
                  >
                    Most Popular
                  </div>
                )}
                <h3 className="text-xl font-semibold mb-2">{plan.name}</h3>
                <p className="text-sm mb-4" style={{ color: '#64748B' }}>{plan.description}</p>
                <div className="mb-6">
                  <span className="text-4xl font-bold">{plan.price}</span>
                  <span className="text-sm ml-2" style={{ color: '#64748B' }}>{plan.period}</span>
                </div>
                <ul className="space-y-3 mb-8">
                  {plan.features.map((feature) => (
                    <li key={feature} className="flex items-start gap-3">
                      <Check className="w-5 h-5 flex-shrink-0 mt-0.5" style={{ color: '#84CC16' }} />
                      <span className="text-sm" style={{ color: '#F8FAFC' }}>{feature}</span>
                    </li>
                  ))}
                </ul>
                <a
                  href={plan.name === 'Enterprise' ? 'mailto:sales@flowdesk.io' : '#pricing'}
                  className="block w-full text-center py-3 rounded-lg font-semibold transition-all hover:scale-105"
                  style={{
                    backgroundColor: plan.highlighted ? '#84CC16' : 'transparent',
                    color: plan.highlighted ? '#0A1628' : '#F8FAFC',
                    border: plan.highlighted ? 'none' : '1px solid rgba(100, 116, 139, 0.3)'
                  }}
                >
                  {plan.cta}
                </a>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Testimonials Carousel */}
      <section id="testimonials" className="py-20 px-4 sm:px-6 lg:px-8" style={{ backgroundColor: '#132240' }}>
        <div className="max-w-4xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
              Loved by teams worldwide
            </h2>
            <p className="text-lg" style={{ color: '#64748B' }}>
              See what our customers have to say
            </p>
          </div>

          <div className="relative">
            <div
              className="p-8 sm:p-12 rounded-2xl border"
              style={{
                backgroundColor: 'rgba(10, 22, 40, 0.5)',
                borderColor: 'rgba(100, 116, 139, 0.2)'
              }}
            >
              <div className="flex gap-1 mb-6">
                {Array(testimonials[testimonialIndex].rating).fill(0).map((_, i) => (
                  <Star key={i} className="w-5 h-5 fill-current" style={{ color: '#84CC16' }} />
                ))}
              </div>
              <blockquote className="text-xl sm:text-2xl font-medium mb-8 leading-relaxed">
                &ldquo;{testimonials[testimonialIndex].quote}&rdquo;
              </blockquote>
              <div>
                <div className="font-semibold">{testimonials[testimonialIndex].role}</div>
                <div style={{ color: '#64748B' }}>{testimonials[testimonialIndex].company}</div>
              </div>
            </div>

            <div className="flex items-center justify-center gap-4 mt-8">
              <button
                onClick={prevTestimonial}
                className="p-3 rounded-full border transition-all hover:border-opacity-50"
                style={{ borderColor: 'rgba(100, 116, 139, 0.3)' }}
                aria-label="Previous testimonial"
              >
                <ChevronLeft className="w-5 h-5" />
              </button>
              <div className="flex gap-2">
                {testimonials.map((_, i) => (
                  <button
                    key={i}
                    onClick={() => setTestimonialIndex(i)}
                    className="w-2 h-2 rounded-full transition-all"
                    style={{
                      backgroundColor: i === testimonialIndex ? '#84CC16' : 'rgba(100, 116, 139, 0.3)'
                    }}
                    aria-label={`Go to testimonial ${i + 1}`}
                  />
                ))}
              </div>
              <button
                onClick={nextTestimonial}
                className="p-3 rounded-full border transition-all hover:border-opacity-50"
                style={{ borderColor: 'rgba(100, 116, 139, 0.3)' }}
                aria-label="Next testimonial"
              >
                <ChevronRight className="w-5 h-5" />
              </button>
            </div>
          </div>
        </div>
      </section>

      {/* FAQ Accordion */}
      <section id="faq" className="py-20 px-4 sm:px-6 lg:px-8">
        <div className="max-w-3xl mx-auto">
          <div className="text-center mb-16">
            <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
              Frequently asked questions
            </h2>
            <p className="text-lg" style={{ color: '#64748B' }}>
              Everything you need to know about FlowDesk
            </p>
          </div>

          <div className="space-y-4">
            {faqs.map((faq, index) => (
              <div
                key={index}
                className="rounded-xl border overflow-hidden"
                style={{
                  backgroundColor: 'rgba(19, 34, 64, 0.5)',
                  borderColor: 'rgba(100, 116, 139, 0.2)'
                }}
              >
                <button
                  onClick={() => setOpenFaq(openFaq === index ? null : index)}
                  className="w-full flex items-center justify-between p-6 text-left"
                >
                  <span className="font-semibold pr-4">{faq.question}</span>
                  {openFaq === index ? (
                    <ChevronUp className="w-5 h-5 flex-shrink-0" style={{ color: '#84CC16' }} />
                  ) : (
                    <ChevronDown className="w-5 h-5 flex-shrink-0" style={{ color: '#64748B' }} />
                  )}
                </button>
                <div
                  className={`overflow-hidden transition-all duration-300 ${
                    openFaq === index ? 'max-h-96' : 'max-h-0'
                  }`}
                >
                  <p className="px-6 pb-6" style={{ color: '#64748B' }}>
                    {faq.answer}
                  </p>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 px-4 sm:px-6 lg:px-8" style={{ backgroundColor: '#132240' }}>
        <div className="max-w-4xl mx-auto text-center">
          <h2 className="text-3xl sm:text-4xl font-bold mb-4" style={{ fontFamily: 'Inter, sans-serif' }}>
            Ready to boost your team productivity?
          </h2>
          <p className="text-lg mb-8" style={{ color: '#64748B' }}>
            Start your 14 day free trial today. No credit card required.
          </p>
          <a
            href="#pricing"
            className="inline-flex items-center gap-2 px-8 py-4 rounded-lg text-base font-semibold transition-all hover:scale-105"
            style={{ backgroundColor: '#84CC16', color: '#0A1628' }}
          >
            Get Started Free
            <ArrowRight className="w-5 h-5" />
          </a>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-12 px-4 sm:px-6 lg:px-8 border-t" style={{ borderColor: 'rgba(100, 116, 139, 0.2)' }}>
        <div className="max-w-7xl mx-auto">
          <div className="grid grid-cols-2 md:grid-cols-4 gap-8 mb-12">
            <div className="col-span-2 md:col-span-1">
              <a href="#" className="flex items-center gap-2 mb-4">
                <div className="w-8 h-8 rounded-lg flex items-center justify-center" style={{ backgroundColor: '#84CC16' }}>
                  <Kanban className="w-5 h-5" style={{ color: '#0A1628' }} />
                </div>
                <span className="text-xl font-bold" style={{ fontFamily: 'Inter, sans-serif' }}>FlowDesk</span>
              </a>
              <p className="text-sm mb-4" style={{ color: '#64748B' }}>
                Project management built for remote teams.
              </p>
              <div className="flex gap-4">
                <a href="#" aria-label="Twitter" className="transition-colors hover:opacity-80">
                  <Twitter className="w-5 h-5" style={{ color: '#64748B' }} />
                </a>
                <a href="#" aria-label="LinkedIn" className="transition-colors hover:opacity-80">
                  <Linkedin className="w-5 h-5" style={{ color: '#64748B' }} />
                </a>
                <a href="#" aria-label="GitHub" className="transition-colors hover:opacity-80">
                  <Github className="w-5 h-5" style={{ color: '#64748B' }} />
                </a>
              </div>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Product</h4>
              <ul className="space-y-2">
                <li><a href="#features" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Features</a></li>
                <li><a href="#pricing" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Pricing</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Integrations</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Changelog</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Company</h4>
              <ul className="space-y-2">
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>About</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Blog</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Careers</a></li>
                <li><a href="mailto:hello@flowdesk.io" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Contact</a></li>
              </ul>
            </div>
            <div>
              <h4 className="font-semibold mb-4">Support</h4>
              <ul className="space-y-2">
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Help Center</a></li>
                <li><a href="#faq" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>FAQ</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Privacy</a></li>
                <li><a href="#" className="text-sm transition-colors hover:opacity-80" style={{ color: '#64748B' }}>Terms</a></li>
              </ul>
            </div>
          </div>
          <div className="pt-8 border-t flex flex-col md:flex-row items-center justify-between gap-4" style={{ borderColor: 'rgba(100, 116, 139, 0.2)' }}>
            <p className="text-sm" style={{ color: '#64748B' }}>
              &copy; 2024 FlowDesk. All rights reserved.
            </p>
            <a
              href="mailto:hello@flowdesk.io"
              className="flex items-center gap-2 text-sm transition-colors hover:opacity-80"
              style={{ color: '#64748B' }}
            >
              <Mail className="w-4 h-4" />
              hello@flowdesk.io
            </a>
          </div>
        </div>
      </footer>
    </div>
  )
}
```

### app/globals.css
```tsx
@import "tailwindcss";

@theme {
  --color-primary: #0A1628;
  --color-secondary: #132240;
  --color-accent: #84CC16;
  --color-background: #F8FAFC;
}

@layer base {
  * { box-sizing: border-box; }
  body { margin: 0; }
}

```

### app/layout.tsx
```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'FlowDesk',
  description: 'Plataforma SaaS de gestión de proyectos para equipos remotos con tableros kanban, seguimiento de tiempo, reportes automáticos e integraciones con Slack y GitHub.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body className="min-h-screen bg-white text-gray-900" style={{ backgroundColor: '#0A1628' }}>
        {children}
      </body>
    </html>
  );
}

```
